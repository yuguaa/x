import type { ComponentToken as HighlightCodeToken } from '../../HighlightCode/style';
import type { ComponentToken as MermaidToken } from '../../Mermaid/style';

export interface ComponentTokenMap {
  Mermaid: MermaidToken;
  HighlightCode: HighlightCodeToken;
}
